name: Rust Security Scan

on:
  workflow_call:
    inputs:
      repo-path:
        description: 'Path to the repository to scan'
        required: false
        default: '.'
        type: string

    secrets:
      SNYK_SCANNER_TOKEN:
        required: true
      SNYK_SCANNER_REGION:
        required: true
      SNYK_GLOBAL_POLICY:
        required: true

jobs:
  rust-scan:
    name: Rust Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Detect Project Type
        id: detect
        shell: bash
        run: |
          REPO_PATH="${{ inputs.repo-path }}"
          if [ ! -f "${REPO_PATH}/Cargo.toml" ]; then
            echo "::warning::No Cargo.toml found - not a Rust project"
            echo "is_rust=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "Project Type : Rust"
          echo "is_rust=true" >> $GITHUB_OUTPUT

      - name: 📦 Install system libraries
        if: steps.detect.outputs.is_rust == 'true'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y pkg-config libdbus-1-dev

      - name: 🦀 Set up Rust Toolchain with components
        if: steps.detect.outputs.is_rust == 'true'
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: clippy, rustfmt

      - name: ⚡ Cache Cargo Registry
        id: cache-cargo
        if: steps.detect.outputs.is_rust == 'true'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: ⚡ Cache Target
        id: cache-target
        if: steps.detect.outputs.is_rust == 'true'
        uses: actions/cache@v4
        with:
          path: |
            **/target
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-target-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-target-

      - name: ⚡ Cache Cargo Bin
        id: cache-bin
        if: steps.detect.outputs.is_rust == 'true'
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: ${{ runner.os }}-cargo-bin-v2

      - name: ⚡ Cache Global NPM Tools
        id: cache-npm
        if: steps.detect.outputs.is_rust == 'true'
        uses: actions/cache@v4
        with:
          path: ~/.npm-global
          key: ${{ runner.os }}-npm-tools-v1

      - name: 📦 Install Rust analysis tools
        if: steps.detect.outputs.is_rust == 'true'
        shell: bash
        run: |
          # Only install if missing
          if ! command -v cargo-cyclonedx >/dev/null 2>&1; then
            cargo install cargo-cyclonedx
          fi
          if ! command -v cargo-audit >/dev/null 2>&1; then
            cargo install cargo-audit
          fi

      - name: 📦 Install required tools
        if: steps.detect.outputs.is_rust == 'true'
        shell: bash
        run: |
          if ! command -v snyk > /dev/null; then
            mkdir -p ~/.npm-global
            npm config set prefix ~/.npm-global
            npm install -g snyk@latest
            echo "$HOME/.npm-global/bin" >> $GITHUB_PATH
          fi

      - name: 📦 Install yq
        if: steps.detect.outputs.is_rust == 'true'
        run: |
          sudo wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/bin/yq
          sudo chmod +x /usr/bin/yq

      - name: 🔐 Authentication
        if: steps.detect.outputs.is_rust == 'true'
        working-directory: ${{ inputs.repo-path }}
        shell: bash
        env:
          SNYK_SCANNER_TOKEN: ${{ secrets.SNYK_SCANNER_TOKEN }}
          SNYK_SCANNER_REGION: ${{ secrets.SNYK_SCANNER_REGION }}
        run: |
          echo "::add-mask::$SNYK_SCANNER_TOKEN"
          echo "::add-mask::$SNYK_SCANNER_REGION"
          snyk config set endpoint="https://api.${SNYK_SCANNER_REGION}.snyk.io" >/dev/null 2>&1
          snyk auth "$SNYK_SCANNER_TOKEN" >/dev/null 2>&1

      - name: 🛡️ Run Cargo Audit (RustSec)
        if: steps.detect.outputs.is_rust == 'true'
        working-directory: ${{ inputs.repo-path }}
        shell: bash
        continue-on-error: true
        run: |
          echo "Running cargo audit..."
          cargo audit --json > audit-result.json || true

          if [ ! -s audit-result.json ]; then
            echo "::warning::⚠️ cargo audit produced no output, generating empty result"
            echo '{"vulnerabilities":{"list":[],"count":0},"warnings":[]}' > audit-result.json
          fi

          if ! jq empty audit-result.json; then
            echo "::warning::⚠️ Invalid Cargo Audit JSON"
            exit 1
          fi

      - name: 🔎 Snyk Code Test
        if: steps.detect.outputs.is_rust == 'true'
        working-directory: ${{ inputs.repo-path }}
        shell: bash
        continue-on-error: true
        run: |
          echo "Running Snyk Code Test..."
          SNYK_CODE_EXIT=0
          snyk code test --json > snyk-code-result.json || SNYK_CODE_EXIT=$?

          if [[ "$SNYK_CODE_EXIT" -ne 0 ]] && [ ! -f "snyk-code-result.json" ]; then
            echo '{"issues":[]}' > snyk-code-result.json
          fi

          if ! jq empty snyk-code-result.json 2>/dev/null; then
            echo "::warning::⚠️ Invalid Snyk Code JSON"
            exit 1
          fi

      - name: 🧪 Run SBOM Scan and Check Vulnerability
        if: steps.detect.outputs.is_rust == 'true'
        working-directory: ${{ inputs.repo-path }}
        shell: bash
        env: 
          SNYK_GLOBAL_POLICY: ${{ secrets.SNYK_GLOBAL_POLICY }}
        continue-on-error: true
        run: |
          # Generate SBOM
          if ! cargo cyclonedx --format json > cargo-sbom.json 2>/dev/null; then
            echo "::warning::⚠️ cargo cyclonedx exited with error — checking SBOM validity"
            if [ ! -s cargo-sbom.json ]; then
              echo "::warning::⚠️ SBOM generation failed completely"
              exit 1
            fi
          fi

          if ! jq empty cargo-sbom.json 2>/dev/null; then
            echo "::warning::⚠️ Invalid SBOM JSON"
            exit 1
          fi

          echo "----------------------------------------------------------------------"
          echo "📦 Generated SBOM"

          # Run Snyk SBOM test
          SNYK_EXIT=0
          snyk sbom test --experimental --file cargo-sbom.json --org=affinidi --json > sbom-result.json || SNYK_EXIT=$?

          if [[ "$SNYK_EXIT" -ne 0 ]] && [ ! -f "sbom-result.json" ]; then
            echo "::warning::⚠️ Snyk scan failed and no results file was generated"
            exit 1
          fi

          echo "----------------------------------------------------------------------"
          echo "✓ Scanning Finished"

          # Extract ignored CVEs from local and global policies
          LOCAL_POLICY="${{ inputs.repo-path }}/.snyk"
          LOCAL_IGNORES="[]"
          GLOBAL_POLICY_PATH="./.snyk-global"
          GLOBAL_IGNORES="[]"

          # Extract ignores as proper JSON array (sorted by year + number)
          extract_ignores() {
            local file=$1
            
            # Return empty array if file doesn't exist
            if [ ! -f "$file" ]; then
              echo "[]"
              return
            fi
            
            # Get ignore keys, handling empty objects gracefully
            local cve_list
            cve_list=$(yq '.ignore | keys | .[]' "$file" 2>/dev/null | tr '[:lower:]' '[:upper:]' | sort -t'-' -k2,2n -k3,3n)
            
            # Convert to JSON array
            if [ -n "$cve_list" ]; then
              echo "$cve_list" | jq -R . | jq -s .
            else
              echo "[]"
            fi
          }

          # Check local policy
          if [ -f "$LOCAL_POLICY" ]; then
            LOCAL_IGNORES=$(extract_ignores "$LOCAL_POLICY")
            LOCAL_COUNT=$(echo "$LOCAL_IGNORES" | jq 'length')

            echo "----------------------------------------------------------------------"
            echo "🗂 Found local .snyk file, ignoring $LOCAL_COUNT CVE(s)"
            i=1
            for cve in $(echo "$LOCAL_IGNORES" | jq -r '.[]'); do
              echo "  $i) $cve"
              i=$((i+1))
            done
          else
            echo "----------------------------------------------------------------------"
            echo "ℹ️ No local .snyk file found"
          fi

          # Check global policy
          if [ -n "$SNYK_GLOBAL_POLICY" ]; then
            curl -sSL "$SNYK_GLOBAL_POLICY" -o "$GLOBAL_POLICY_PATH" || echo "⚠️ Failed to fetch global policy"
          fi

          if [ -f "$GLOBAL_POLICY_PATH" ]; then
            GLOBAL_IGNORES=$(extract_ignores "$GLOBAL_POLICY_PATH")
            GLOBAL_COUNT=$(echo "$GLOBAL_IGNORES" | jq 'length')

            echo "----------------------------------------------------------------------"
            echo "🗂 Found global .snyk-global file, ignoring $GLOBAL_COUNT CVE(s)"
            i=1
            for cve in $(echo "$GLOBAL_IGNORES" | jq -r '.[]'); do
              echo "  $i) $cve"
              i=$((i+1))
            done
          else
            echo "----------------------------------------------------------------------"
            echo "ℹ️ No global .snyk-global file available"
          fi

          # Merge for filtering
          IGNORED_CVES_JSON=$(jq -n --argjson local "$LOCAL_IGNORES" --argjson global "$GLOBAL_IGNORES" '$local + $global')

          # Combine all results
          jq -s '{cargo_audit: .[0], snyk: .[1], code_test: .[2]}' audit-result.json sbom-result.json snyk-code-result.json > combined-result.json

          # Normalize CVEs in SBOM to uppercase and filter ignored ones
          jq --argjson ignored "$IGNORED_CVES_JSON" '
            .snyk.vulnerabilities |= (if . then map(
              if .identifiers.CVE then
                .identifiers.CVE |= map(ascii_upcase)
              else
                .
              end
            ) | map(select(
              (.identifiers.CVE // []) | all(. as $cve | $ignored | index($cve) | not)
            )) else [] end)
          ' combined-result.json > combined-result-filtered.json

          # Count vulnerabilities by severity
          audit_critical=$(jq '[.cargo_audit.vulnerabilities.list[]? | select(.advisory.severity=="critical")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          audit_high=$(jq '[.cargo_audit.vulnerabilities.list[]? | select(.advisory.severity=="high")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          snyk_critical=$(jq '[.snyk.vulnerabilities? // [] | .[] | select(.severity=="critical")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          snyk_high=$(jq '[.snyk.vulnerabilities? // [] | .[] | select(.severity=="high")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          code_critical=$(jq '[.code_test.issues[]? | select(.severity=="critical")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          code_high=$(jq '[.code_test.issues[]? | select(.severity=="high")] | length' combined-result-filtered.json 2>/dev/null || echo 0)
          
          total_critical=$((audit_critical + snyk_critical + code_critical))
          total_high=$((audit_high + snyk_high + code_high))
          unknown=$(jq '[.snyk.vulnerabilities? // [] | .[] | select(.severity=="unknown")] | length' combined-result-filtered.json 2>/dev/null || echo 0)

          if [[ $total_critical -gt 0 || $total_high -gt 0 ]]; then
            echo "----------------------------------------------------------------------"
            echo "                        VULNERABILITY DETAILS          "
            echo "----------------------------------------------------------------------"
            
            # Display Cargo Audit vulnerabilities
            if [[ $audit_critical -gt 0 || $audit_high -gt 0 ]]; then
              echo "🦀 CARGO AUDIT (RustSec) FINDINGS:"
              jq -r '
                (.cargo_audit.vulnerabilities.list // [])
                | map(select(.advisory.severity == "critical" or .advisory.severity == "high"))
                | .[]
                | "\(.advisory.severity | ascii_upcase) | \(.package.name)@\(.package.version)\n" +
                  "📛 Title: \(.advisory.title)\n" +
                  "🔗 CVE: \(.advisory.aliases[0] // "None")\n" +
                  "⚠️ CVSS: \(.advisory.cvss // "?") \n" +
                  "💡 Fix: \(if .versions.patched then "Upgrade to: \(.versions.patched | join(", "))" else "See: \(.advisory.url // "No fix info")" end)\n" +
                  "---"
              ' combined-result-filtered.json
            fi

            # Display Snyk SBOM vulnerabilities  
            if [[ $snyk_critical -gt 0 || $snyk_high -gt 0 ]]; then
              echo "🔍 SNYK SBOM FINDINGS:"
              jq -r '
                (.snyk.vulnerabilities // [])
                | map(select(.severity == "critical" or .severity == "high"))
                | .[]
                | "\(.severity | ascii_upcase) | \(.packageName)@\(.version)\n" +
                  "📛 Title: \(.title)\n" +
                  "🔗 CVE: \(.identifiers.CVE[0] // "None")\n" +
                  "⚠️ CVSS: \(.cvssScore // "?") (\(.CVSSv3 // "No vector"))\n" +
                  "💡 Fix: \(if .semver.patched then "Upgrade to \(.semver.patched[0])" 
                        elif .semver.unaffected then "Use version: \(.semver.unaffected[0])" 
                        else "See: \(.url // "No fix info")" end)\n" +
                  "---"
              ' combined-result-filtered.json
            fi

            # Display Snyk Code Test issues
            if [[ $code_critical -gt 0 || $code_high -gt 0 ]]; then
              echo "🔎 SNYK CODE TEST FINDINGS:"
              jq -r '
                (.code_test.issues // [])
                | map(select(.severity == "critical" or .severity == "high"))
                | .[]
                | "\(.severity | ascii_upcase) | \(.id)\n" +
                  "📛 Title: \(.title)\n" +
                  "📍 File: \(.filePath):\(.lineNumber // "?")\n" +
                  "💡 Fix: \(.fixedIn // "Manual review required")\n" +
                  "---"
              ' combined-result-filtered.json
            fi

            echo "::warning::┌────────────────────────────────────────────────────────────────────────────────────────────────────────"
            echo "::warning::│ CRITICAL SECURITY ISSUES DETECTED                   "
            echo "::warning::├────────────────────────────────────────────────────────────────────────────────────────────────────────"
            echo "::warning::│ 🟣 Critical vulnerabilities: $total_critical              "
            echo "::warning::│ 🔴 High vulnerabilities: $total_high                      "
            echo "::warning::│                                                     "
            echo "::warning::│ Review the detailed report above and either:        "
            echo "::warning::│                                                     "
            echo "::warning::│ 1. Fix Vulnerabilities OR                           "
            echo "::warning::│ 2. Consult Security incase of false positives / risk acceptance, update .snyk policy file accordingly"
            echo "::warning::└────────────────────────────────────────────────────────────────────────────────────────────────────────"
            exit 1

          elif [[ "$unknown" -gt 0 ]]; then
            echo "::warning::⚪ Scan completed with $unknown unknown severity vulnerabilities"
          else
            echo "----------------------------------------------------------------------"
            echo "✅ All good, no critical or high vulnerabilities found. Good job! 🎉"
            echo "----------------------------------------------------------------------"
          fi

      - name: 🧹 Cleanup
        if: always() && steps.detect.outputs.is_rust == 'true'
        working-directory: ${{ inputs.repo-path }}
        shell: bash
        run: |
          rm -f cargo-sbom.json sbom-result.json audit-result.json combined-result.json combined-result-filtered.json snyk-code-result.json

      - name: ℹ️ Skip if not a Rust project
        if: steps.detect.outputs.is_rust != 'true'
        shell: bash
        run: echo "🟡 Skipping scan - not a Rust project"